- defaults:

    link:
      relink: true

- clean: ['~']

- link:
    ~/.gitconfig: gitconfig
    ~/.zshrc: zshrc

- create:
    - ~/projetos

# - shell:
#   - [git submodule update --init --recursive, Installing submodules]
